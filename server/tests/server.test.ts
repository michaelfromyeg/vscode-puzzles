import { describe, it } from "mocha";
import { expect } from "chai";

describe("SANITY *", () => {

  it("Verify the test setup is working", () => {
    expect(false).to.be.false;
    expect(true).to.be.true;
  });

});
